-- LibEditMode placeholder
local MAJOR, MINOR = "LibEditMode", 1
local lib = LibStub:NewLibrary(MAJOR, MINOR)

if not lib then return end

function lib:RegisterFrame(frame, name, system)
    -- Placeholder function that would normally register a frame with the edit mode
    return
end