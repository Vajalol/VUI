-- LibEditModeOverride placeholder
local MAJOR, MINOR = "LibEditModeOverride", 1
local lib = LibStub:NewLibrary(MAJOR, MINOR)

if not lib then return end

function lib:RegisterFrame(frame, editModeSystem)
    -- Placeholder function that would normally register a frame with the edit mode override
    return
end